const { useState } from "react"
import {
	View,
	Text,
	TextInput,
	TouchableOpacity,
	FlatList,
	StyleSheet,
} from "react-native";

const = () => {
	const[ task, setTask] = useState("") ;
	const[tasks, setTasks] = useState([]);
}
